interface:
  display_name: "Callit Worker"
  short_description: "通过 Callit MCP 管理 Worker 的完整工作流"
  default_prompt: "使用 $callit-worker 通过已配置的 callit-mcp 创建、更新或维护 Callit Worker。"

dependencies:
  tools:
    - type: "mcp"
      value: "callit-mcp"
      description: "Callit Worker 管理 MCP 服务"
      transport: "streamable_http"
      url: "https://c.xiaosm.cn/mcp"

policy:
  allow_implicit_invocation: true
